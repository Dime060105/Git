numbers = [1, 2, 3, 4, 5]
numbers.insert(0, -1) # insersce degli elementi all'interno di una lista
numbers.remove(3) # rimuove degli elementi all'interno di una lista
# numbers.clear() # rimuove tutti gli elementi
print(numbers)
print(10 in numbers) # vede se 10 è nella lista
print(len(numbers)) # vede quanti elementi ci sono in una lista